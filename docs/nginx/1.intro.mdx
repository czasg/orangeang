---
title: 前言
hide_title: false
hide_table_of_contents: false
slug: /
---

<br/>

<div align="center">
  <img src={require('./nginx-logo.png').default}></img>
</div>

<br/>
<br/>

Nginx 是世界上最流行的应用软件之一，几乎每三个网站中就有一个使用了 Nginx。

它的特点是高性能和支持热部署，可以在不中断服务的情况下对配置进行升级。因此 Nginx 被广泛应用于多种场景下，包括：

- **API网关**：在微服务体系下，配合 Lua 实现限流、熔断等网关功能。
- **反向代理服务器**：对内屏蔽真实服务器地址，对外提供请求转发服务。
- **负载均衡服务器**：对外提供统一入口，对内将流量分布到多个服务器上。
- **文件服务器**：对内关联静态文件，对外提供网络文件服务。

## 安装

本教程中关于 nginx 的演示都是基于 docker 完成的，因此请先确保 [安装docker](#)。

安装好 docker 后就可以执行以下操作进行验证。

1. 拉取nginx镜像

```bash
$ docker pull nginx
```

2. 查看nginx版本信息

```bash
$ docker run -it --rm --entrypoint=nginx nginx -V
```

```text title="版本信息"
nginx version: nginx/1.21.5
built by gcc 10.2.1 20210110 (Debian 10.2.1-6)
built with OpenSSL 1.1.1k  25 Mar 2021
TLS SNI support enabled
configure arguments: ...
```

如果可以看到类似的输出即表明一切已经准备ok啦~


<br/>

:::info 👇👇👇
**本文作者:** 橙子昂 <br/>
**版权声明:** 转载请注明出处哦~👮‍
:::
